import string from './models/Search';

// import * as searchView from './views/searchView';  import all statement 
import { add, multiply, ID } from './views/searchView';

console.log(`${string}, This is a function ${add(1,2)}, here is a multiply function ${multiply(ID,4)}, here is an id ${ID}`);
